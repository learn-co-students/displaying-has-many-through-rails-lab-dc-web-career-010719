<h2>Doctor Name: <%= @doctor.name %></h2>
<h2>Doctor Department: <%= @doctor.department %></h2>
<% @doctor.appointments.each do |appt| %>
<%= link_to appt.patient.name, patient_path(appt.patient) %>
<%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br>
<% end %>