<h2>Patient: <%= @patient.name %></h2>
<% @patient.appointments.each do |apt|%>
Dr. <%= link_to apt.doctor.name, doctor_path(apt.doctor) %>
<%= apt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br>
<% end %>
