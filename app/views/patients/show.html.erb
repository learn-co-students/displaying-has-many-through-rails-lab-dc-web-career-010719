
<% @patient.appointments.each do |appointment| %>
- <%= appointment.appointment_datetime.strftime("%B %d, %Y")%> at <%= appointment.appointment_datetime.strftime("%H:%M") %> Doctor: <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %><br>
<% end %>
